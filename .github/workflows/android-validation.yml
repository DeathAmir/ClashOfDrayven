name: Android Native Validation

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'ANDROID_VALIDATION.md'
      - 'BUILD_STATUS.md'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    name: Compile Android C++ R8 NDK and zipalign
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - uses: android-actions/setup-android@v3
      - name: Install SDK and NDK
        run: yes | sdkmanager "platforms;android-35" "build-tools;35.0.0" "ndk;27.0.12077973" "cmake;3.22.1"
      - name: Prepare branded assets and packs
        run: |
          python -m pip install --disable-pip-version-check pillow
          python scripts/fetch_assets.py
          python scripts/prepare_branding.py
          dotnet restore Tools/ClDryPacker/ClDryPacker.csproj
          dotnet build Tools/ClDryPacker/ClDryPacker.csproj -c Release --no-restore
          mkdir -p dist/packs
          dotnet run --project Tools/ClDryPacker/ClDryPacker.csproj -c Release --no-build -- pack20 build-assets/PackSource dist/packs
          python scripts/prepare_android_assets.py
      - name: Compile release APK AAB and DrayvenEngine
        run: gradle -p android --no-daemon :app:assembleRelease :app:bundleRelease
      - name: Zipalign and verify package structure
        run: |
          set -euo pipefail
          BT="$ANDROID_HOME/build-tools/35.0.0"
          APK=android/app/build/outputs/apk/release/app-release-unsigned.apk
          AAB=android/app/build/outputs/bundle/release/app-release.aab
          test -f "$APK"
          test -f "$AAB"
          mkdir -p dist/validation dist/signing-tools/lib
          "$BT/zipalign" -f -p 4 "$APK" dist/validation/ClashOfDrayven-Android-aligned-unsigned.apk
          "$BT/zipalign" -c -v 4 dist/validation/ClashOfDrayven-Android-aligned-unsigned.apk
          unzip -l dist/validation/ClashOfDrayven-Android-aligned-unsigned.apk | grep 'lib/arm64-v8a/libclashofdrayven.so'
          cp "$AAB" dist/validation/ClashOfDrayven-Android-unsigned-validation.aab
          unzip -j dist/validation/ClashOfDrayven-Android-aligned-unsigned.apk 'lib/arm64-v8a/libclashofdrayven.so' -d dist/validation
          cp "$BT/apksigner" dist/signing-tools/apksigner
          cp "$BT/zipalign" dist/signing-tools/zipalign
          cp "$BT/lib/apksigner.jar" dist/signing-tools/lib/apksigner.jar
          chmod +x dist/signing-tools/apksigner dist/signing-tools/zipalign
      - uses: actions/upload-artifact@v4
        with:
          name: android-aligned-validation
          path: |
            dist/validation/*
            dist/signing-tools/*
            dist/signing-tools/lib/*
          if-no-files-found: error

  report:
    name: Record Android validation result
    needs: [validate]
    if: always() && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Write validation status
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git reset --hard origin/main
          cat > ANDROID_VALIDATION.md <<EOF
          # Android Validation

          - Workflow run: ${GITHUB_RUN_ID}
          - Source commit: ${GITHUB_SHA}
          - Compile result: ${{ needs.validate.result }}
          - Pipeline: C++/NDK + R8 + zipalign
          - Run URL: https://github.com/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}
          EOF
          git config user.name "IrAutoX Build Bot"
          git config user.email "actions@users.noreply.github.com"
          git add ANDROID_VALIDATION.md
          git commit -m "Record Android validation [skip ci]" || exit 0
          git push origin HEAD:main
