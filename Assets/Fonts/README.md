# Licensed UI font

Place your licensed copy of `Supercell-Magic_5.ttf` in this folder before building if you want that typeface used by the splash screen and the full UI.

The game loads it automatically at runtime. If it is absent, a Windows fallback font is used so builds remain reproducible.

The font binary is intentionally not redistributed by this repository.
